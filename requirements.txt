# AI 메타표면 설계 시스템 v3.0 - Python 패키지 의존성

# 웹 프레임워크
streamlit>=1.28.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0

# 시각화
plotly>=5.17.0

# AI/ML
torch>=2.0.0
scikit-learn>=1.3.0

# 데이터베이스
sqlite3  # Python 표준 라이브러리 (별도 설치 불필요)

# HTTP 요청
requests>=2.31.0
urllib3>=2.0.0

# XML/HTML 파싱
beautifulsoup4>=4.12.0
lxml>=4.9.0

# API 클라이언트
scholarly>=1.7.0  # Google Scholar (선택)
crossref-commons>=0.0.7  # CrossRef
semanticscholar>=0.3.0  # Semantic Scholar

# Kaggle (선택)
kaggle>=1.5.16

# 유틸리티
python-dateutil>=2.8.0
tqdm>=4.66.0  # 진행 표시
configparser>=6.0.0  # 설정 파일 파싱

# 과학 계산
scipy>=1.11.0

# 이미지 처리 (향후 확장)
pillow>=10.0.0

# JSON 처리 (표준 라이브러리지만 명시)
# json  # Python 표준 라이브러리

# 날짜/시간
pytz>=2023.3

# 보안/인증
cryptography>=41.0.0  # API 토큰 암호화용

# 테스팅 (개발용)
pytest>=7.4.0
pytest-cov>=4.1.0

# 코드 품질 (개발용)
black>=23.7.0
flake8>=6.1.0
isort>=5.12.0

# 타입 힌트 (개발용)
mypy>=1.5.0

# 문서화 (개발용)
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# Jupyter (분석용, 선택)
jupyter>=1.0.0
jupyterlab>=4.0.0

# 환경 변수 관리
python-dotenv>=1.0.0

# 파일 압축/해제
zipfile36>=0.1.3  # Python 3.6+용
